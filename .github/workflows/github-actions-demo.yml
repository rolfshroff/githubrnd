name: GitHub Actions Demo
on: 
  push:
  workflow_dispatch:
  
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Save secret
        env: 
          GITHUB_TOKEN: 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
          My_Secret: Rolf
        run: |
          gh repo list
          # gh secret list
          gh secret set MYSECRET2 -b"${My_Secret}" --repo "rolfshroff/githubrnd"      
